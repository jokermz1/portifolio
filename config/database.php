<?php
// As credenciais da base de dados (DB_HOST, DB_NAME, DB_USER, DB_PASS,
// DB_CHARSET) estão definidas em config/env.php — ficheiro não versionado.
// require_once garante que são carregadas mesmo que este ficheiro seja
// incluído isoladamente (ex.: scripts de migração).
require_once __DIR__ . '/env.php';
